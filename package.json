{
  "name": "SikhiToTheMax-Web",
  "version": "1.0.0",
  "description": "Sevadaars are currently working hard to build newer version of SikhiToTheMax website using modern web technologies.",
  "main": "index.js",
  "scripts": {
    "test": "standard --verbose | snazzy",
    "start": "concurrently --kill-others \"npm run watch-css\" \"npm run babel:dev\" \"lite-server\"",
    "babel": "babel src/js --out-dir assets/js --copy-files --ignore *.min.js",
    "build": "npm run babel",
    "build:prod": "npm run build-css && BABEL_ENV=production npm run babel",
    "babel:dev": "babel src/js --watch --out-dir assets/js --copy-files --ignore *.min.js",
    "deploy": "git checkout dev && git pull origin && git push web",
    "deploy:server": "npm run build:prod && rm -rf .gitignore node_modules src LICENSE package.json README.md router.php",
    "build-css": "node-sass --output-style compressed --include-path src/scss src/scss/style.scss assets/css/bundle.css",
    "watch-css": "nodemon --watch src -e scss -x \"node-sass --output-style nested --source-map true --include-path src/scss src/scss/style.scss assets/css/bundle.css\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KhalisFoundation/SikhiToTheMax-Web.git"
  },
  "keywords": [],
  "author": "",
  "license": "GSL",
  "bugs": {
    "url": "https://github.com/KhalisFoundation/SikhiToTheMax-Web/issues"
  },
  "homepage": "https://github.com/KhalisFoundation/SikhiToTheMax-Web#readme",
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-core": "^6.23.1",
    "babel-preset-env": "^1.1.8",
    "babili": "0.0.11",
    "concurrently": "^3.4.0",
    "node-sass": "^4.5.1",
    "nodemon": "^1.11.0",
    "snazzy": "^6.0.0",
    "standard": "^9.0.1"
  },
  "babel": {
    "env": {
      "production": {
        "presets": [
          "babili"
        ]
      }
    },
    "presets": [
      [
        "env",
        {
          "targets": {
            "browsers": [
              "> 1%",
              "ie 11",
              "safari 9"
            ]
          },
          "modules": false
        }
      ]
    ]
  },
  "standard": {
    "ignore": [
      "assets/js"
    ]
  },
  "dependencies": {
    "lite-server": "^2.3.0"
  }
}
