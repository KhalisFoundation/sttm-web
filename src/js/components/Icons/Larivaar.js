import React from 'react';

export const LarivaarIcon = props => (
  <span onClick={props.onClick} className={`custom-fa ${props.className}`}>ੳਅ</span>
);

export const LarivaarAssistIcon = props => (
  <span onClick={props.onClick} className={`custom-fa  custom-fa-assist ${props.className}`}>ੳ</span>
);
